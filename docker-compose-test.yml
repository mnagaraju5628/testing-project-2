version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=test
      - PORT=8080
    restart: always
    ports:
      - "80:8080"    # Host port 80 → container port 8080
    command: ["node", "index.js"]
